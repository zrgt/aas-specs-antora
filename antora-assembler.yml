root_level: 0
component_versions: '**'
asciidoc:
  attributes:
    source-highlighter: rouge
    pdf-theme: pdf-theme.yml
build:
  command: bundle exec asciidoctor-pdf -r ./extended.rb -r asciidoctor-diagram
  keep_aggregate_source: true
